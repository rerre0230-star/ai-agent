# Bridge AI — 시니어 케어 음성 어시스턴트 프로토타입

React + TypeScript + Vite로 만든 클릭 가능한 프로토타입. 병원 예약·두리발 호출·복약 알림 3개 시나리오가 하나의 공용 대화 템플릿(대기 → 인텐트 확인 → 검색/외부연결 → 확정/실패 → 종료)을 공유하고, 여기에 범용 비서 기능(일정 등록·웹 검색·시간/날짜 안내·인사)도 통합되어 있습니다.

## 실행

```bash
npm install
npm run dev
```

## 구성

- `src/engine.ts` — 대화 상태머신 (인텐트 파싱, 병원 검색/연결, 복약 무응답 에스컬레이션, 범용 일정 등록·검색·시간/날짜 안내 등)
- `src/useSpeech.ts` — 음성 인식(STT)·음성 합성(TTS) 훅 (Web Speech API, 미지원 브라우저는 텍스트로 폴백)
- `src/components/` — 상태 카드, 채팅 로그, 입력창, 보호자 알림 대시보드
- `src/mockData.ts` — 등록 병원 목록, 웹 검색 mock

실제 병원/두리발 연동, 통화 자동화는 목업(mock)이며 UI/대화 흐름 검증용 프로토타입입니다. 웹 검색·일정 등록은 실제로 동작합니다(브라우저 팝업 차단 시 채팅에 뜨는 링크로 직접 열면 됩니다).

`voice-assistant/`에는 동일한 일정·검색·시간/날짜 로직을 가진 독립 실행형 Python CLI 버전이 별도로 있습니다(마이크·스피커가 있는 로컬 환경에서 API 키 없이 음성으로만 쓰고 싶을 때 사용).

## 개발 현황

**완료**
- [x] 공용 대화 템플릿 상태머신: 대기 → 인텐트 확인 → (검색/외부연결) → 확정/실패 → 종료
- [x] 병원 예약: 등록 병원 즉시 연결 / 미등록 병원은 웹 검색 → 번호 재확인 → 연결, 신규 병원 등록 제안
- [x] 두리발(이동수단) 호출
- [x] 복약 알림: 1차 → 재알림(30분→데모 12초로 압축) → 2회 연속 무응답 시 세션 종료 + 보호자 알림
- [x] 재질문 상한 1회 → 초과 시 보호자 확인 요청으로 전환
- [x] 일정(캘린더) 탭: 병원/두리발/복약 결과 + 범용 일정을 "예정"/"기록"으로 표시
- [x] 보호자 알림 탭: 예약확정·미복약·확인요청 등 실시간 알림 로그
- [x] 범용 비서 인텐트 통합: 자연어 일정 등록(날짜/시간 파싱 → 캘린더 기록), 웹 검색, 시간/날짜 안내, 인사
- [x] 음성 입력(STT)·음성 출력(TTS) — Web Speech API, 텍스트 채팅과 상시 동시 사용 가능
- [x] 상단 고정 홈 버튼 — 어느 화면에서든 대화 탭 + 대기 상태로 안전 복귀
- [x] 음성인식 오류 피드백 — 원인별(권한 거부/마이크 없음/네트워크/iframe 임베드 제한) 안내를 토스트+음성으로 제공
- [x] UI/UX 감사 및 보정: 디자인 토큰(spacing/radius/typography), WCAG AA 대비, 포커스 인디케이터, aria-label/live region, 44px 터치영역, 취소 시 상태경합 버그 수정
- [x] 독립 실행형 Python 음성 비서(`voice-assistant/`) — 웹앱과 동일한 일정/검색/시간/날짜 로직

**목업(mock)으로 남아있는 부분**
- [ ] 실제 병원/두리발 전화 연결, 통화 자동화 — 모두 랜덤 성공/실패로 시뮬레이션
- [ ] 병원 대표번호 웹 검색 — 실제 검색 API 대신 규칙 기반 mock
- [ ] 사용자 인증, 서버/DB 연동, 다중 사용자·보호자 계정 분리 — 아직 없음(단일 세션 인메모리 상태)

**알려진 제약**: 자세한 내용은 `CLAUDE.md`의 "알려진 함정" 절 참고 (아티팩트 미리보기 마이크 제한, JS 정규식 단어경계 등).
